# APIキーを書くファイル。絶対にコミットしない。
.env

__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# 診断.bat の出力
診断結果.txt

# 索引データベース（再生成できるのでコミットしない）
*.db
*.db-wal
*.db-shm
index.db

# 検索対象のPDF置き場。PDFと台帳はコミットしないが、
# フォルダと案内ファイルは配布物に含める（利用者が置き場に迷わないように）。
/manuals/*
!/manuals/はじめにお読みください.txt
